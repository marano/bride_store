<h1>Depoimentos</h1>

<table>
  <tr>
    <th>Texto</th>
    <th>Ativo</th>
    <th>Destaque</th>
    <th>Usuário</th>
  </tr>

<% for testimonial in @testimonials %>
  <tr>
    <td><%=h testimonial.body %></td>
    <td><%=h testimonial.active %></td>
    <td><%=h testimonial.featured %></td>
    <td><%=h testimonial.user %></td>
    <td><%= link_to 'Exibir', testimonial %></td>
    <td><%= link_to 'Editar', edit_testimonial_path(testimonial) %></td>
    <td><%= link_to 'Remover', testimonial, :confirm => 'Tem certeza?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<%= link_to 'Novo Depoimento', new_testimonial_path %>
